import { Typography } from "@mui/material";
import React from "react";

export default function page() {
  return (
    <section>
      <Typography variant="h2" component={"h1"} sx={{ marginRight: "20vw" }}>
        Customers
      </Typography>
    </section>
  );
}
